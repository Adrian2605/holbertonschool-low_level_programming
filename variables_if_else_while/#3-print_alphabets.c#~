#include <stdio.h>

/**
 *
 *
 *
 *
 *
 *
 *
 */
int main(void)
{
char lowerletter = 'a'
char upperletter = 'A'

  while (lowerletter <= z)
    {
     putchar(lowerletter);
     lowerletter = lowerletter + 1;
    }
 if (lowerletter == z)
   {
    while (upperletter <= Z)
      {
       putchar(upperletter);
       upperletter = up
      }
   }

  return (0);
}