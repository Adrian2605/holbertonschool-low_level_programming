#include "main.h"
#include <unistd.h>

/**
 * add - Adds two integers.
 * @a: Integer to add.
 * @b: Integer to add.
 *
 * Return: The result of adding the two integers.
 */

int add(int a, int b)
{
int result;

result = (a + b);

return(result);
}
