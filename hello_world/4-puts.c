#include <stdio.h>

/**
 * main - Punto de entrada del programa
 *
 * Description: Imprime el mensaje en la salida standard
 *
 * Return: Siempre cero
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
