#include <stdio.h>

/**
 * main - Punto de entrada del programa
 *
 * Description: Imprime el mensaje y agrega una nueva linea al final
 *
 * Return: Siempre retorna 0
 */
int main(void)
{
  printf("swith proper grammar, but the outcome is a piece of art,\n");
  return (0);
}
