#include <unistd.h>

/**
 * reset_to_98 -  takes a pointer to an int as parameter and updates
 * updates the value it points to to 98.
 * @n: Pointer that the function takes as input.
 *
 * Return: Always void.
 */

void reset_to_98(int *n)
{
*n = 98;

return;
}
